import 'package:flutter/material.dart';

class AddClientSccreens extends StatelessWidget {
  const AddClientSccreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: 
      Text("add client"),
    );
  }
}