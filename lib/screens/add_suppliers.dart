import 'package:flutter/material.dart';

class AddSuppliers extends StatelessWidget {
  const AddSuppliers({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("add  Suppliers"),
    );
  }
}