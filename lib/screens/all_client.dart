import 'package:flutter/material.dart';

class AllClient extends StatelessWidget {
  const AllClient({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: 
      Text("all client"),
    );
  }
}